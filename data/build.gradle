apply plugin: 'kotlin'

dependencies {

    //reference the domain module from the data module
    implementation project(':domain')

    implementation kotlinLibs.values()
    implementation testLibs.values()

}